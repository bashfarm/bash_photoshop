# Utils?

> Utils are the smallest decoupled blocks we have. These should be able to be used anywhere at all times. That means that every util function should be able to have a `unit test`. There isn't a a reason why it can't if it is decoupled enough.

https://stacho.atlassian.net/wiki/spaces/STACHO/pages/8290305/Conventions
